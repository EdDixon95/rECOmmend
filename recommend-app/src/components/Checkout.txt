<form action="" method="post" id="form">
    {% csrf_token %}
    <div>
        <p>Name</p>
        {{form.name}}
        <p>Address Line One</p>
        {{form.street_address}}
        <p>City</p>
        {{form.city}}
        <p>State/County</p>
        {{form.state}}
        <p>Postal Code</p>
        {{form.postcode}}
        <p>Please enter your mobile phone number</p>
        {{form.phone_number}}
    </div>


    <h2>Order summary</h2>
    <div>
        {% for product in products %}
        <div>
            <ul>
                <li>Product: {{ product.product.name }}</li>
                <li>Price: £{{ product.product.price|floatformat:2 }}</li>
                <li>Quantity: {{ product.quantity }}</li>
            </ul>
        </div>
        {% endfor %}
        <div>
            <h3>Products: {{ order.get_cart_items }}</h3>
            <h3>Total: £{{ order.get_cart_total|floatformat:2 }}</h3>
        </div>

        <button type="submit" id="pay-now">Pay now</button>
        <button type="submit "><a href="{% url 'cart' %}">Back to cart</a></button>
    </div>
</form>